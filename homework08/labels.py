from types import SimpleNamespace

labels = SimpleNamespace(
    distance='distance (mm)',
    vergence='vergence angle (degrees)',
    noise='noise sigma (pixels)',
    errors='error statistics',
    translation='translation error (degrees)',
    rotation='rotation error (degrees)',
    structure='structure RMS error (baseline units)',
    median='median',
    deviation='median distance from median'
)
